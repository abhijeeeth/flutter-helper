    <application
        android:label="RuDo"
        tools:replace="android:label"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher" //this line is important
        android:allowBackup="false">//this line is important


        ## add this line  in androidManifest.xml

        //this will help in blocking the data  being backuped on the cloud